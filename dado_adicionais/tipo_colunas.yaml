dados:
  "customers": "INT, VARCHAR, DATE, VARCHAR(10), VARCHAR(40), VARCHAR(50), VARCHAR(20), VARCHAR(80), VARCHAR(10), VARCHAR(20), DATE, VARCHAR(10)"
  "orders": "INT, INT, INT, DECIMAL(10, 2), INT, DECIMAL(4, 2), VARCHAR(20), DATE"
  "products": "INT, DECIMAL(4, 2), DECIMAL(4, 2), VARCHAR(100), VARCHAR(20), VARCHAR(20), DATE"
  "reviews": "INT, INT, INT, INT, VARCHAR(500), DATE"